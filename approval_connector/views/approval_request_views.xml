<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <!-- Custom Approval Request Form View for Community Edition -->
    <record id="approval_request_view_form" model="ir.ui.view">
        <field name="name">approval.request.view.form</field>
        <field name="model">approval.request</field>
        <field name="arch" type="xml">
            <form string="Approval Request">
                <header>
                    <button name="action_approve" string="Approve" type="object" class="btn-primary" invisible="state != 'pending'"/>
                    <button name="action_refuse" string="Refuse" type="object" class="btn-secondary" invisible="state != 'pending'"/>
                    <field name="state" widget="statusbar" statusbar_visible="new,pending,approved,refused"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="category_id"/>
                            <field name="order_id" readonly="1"/>
                        </group>
                        <group>
                            <field name="request_owner_id"/>
                            <field name="company_id"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Approvers">
                            <field name="approver_ids">
                                <list>
                                    <field name="user_id"/>
                                    <field name="status"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <!-- Custom Approval Request List View -->
    <record id="approval_request_view_tree" model="ir.ui.view">
        <field name="name">approval.request.view.tree</field>
        <field name="model">approval.request</field>
        <field name="type">list</field>
        <field name="arch" type="xml">
            <list string="Approval Requests">
                <field name="name"/>
                <field name="category_id"/>
                <field name="order_id"/>
                <field name="request_owner_id"/>
                <field name="state"/>
            </list>
        </field>
    </record>

    <!-- Action for Approval Requests -->
    <record id="approval_request_action" model="ir.actions.act_window">
        <field name="name">Approval Requests</field>
        <field name="res_model">approval.request</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first approval request!
            </p>
        </field>
    </record>
</odoo>
